# Remote sandbox experience is now primary

User decided to skip further local Docker sync work and focus on the remote sandbox experience. Local Docker remains useful as a conceptual bridge, but future lessons should prioritize the remote runner model: upload/sync workspace, run code in a remote container, stream logs/status back, expose preview URL, and clean up runtime resources.
