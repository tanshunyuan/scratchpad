# Curl resume proof works

The user tested the in-memory Node resumable stream with curl and confirmed the POST-start / GET-resume flow works. Future lessons can move from server mechanics to client behavior: receiving the run ID, tracking last event ID, and reconnecting after refresh.
