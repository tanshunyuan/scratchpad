# Run listeners versus event log

The user clarified that `listeners` are in-memory HTTP response objects, not application users, and that a run can have zero listeners while the producer keeps emitting into the event log. Future lessons can use the terms listener, replay, and live subscribe without re-teaching this distinction.
