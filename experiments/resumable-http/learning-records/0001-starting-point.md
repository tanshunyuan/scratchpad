# Starting point: resume cursor is unknown

The user knows the desired platform behavior—POST starts an AI stream and refresh should not lose it—but is unsure what to resume by. Future lessons should start with the stream ID + event ID model before introducing Redis or framework helpers.
