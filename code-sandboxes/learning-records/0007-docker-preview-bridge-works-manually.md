# Docker preview bridge works manually

User successfully ran the sample preview app inside Docker and reached it from the host through the mapped preview port. Future lessons can assume the basic Docker bridge works: bind-mounted workspace, container-owned dependencies, Vite listening on `0.0.0.0`, and iframe/browser access through `localhost:5174`.
