# Mission shifted to Dockerized preview runtime

The mission now includes running preview app code inside Docker from the Electron app, not merely understanding when Docker might become necessary. Future lessons should teach the Docker bridge model: bind-mounted workspace, container dev server, host port mapping, log streaming, cleanup, and sandbox trust boundaries.
